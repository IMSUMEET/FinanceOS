# CI — superseded by Quality Gate workflow
#
# Branch protection should require: "Quality Gate" (see .github/workflows/quality-gate.yml)
# This file is kept for manual triggers only to avoid duplicate CI runs.

name: CI (legacy)

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/quality-gate.yml for PR checks"
